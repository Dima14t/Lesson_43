Создайте класс `Book` с методом `@classmethod`, 
который создает объект из строки "Название,Автор,Год".
    
Добавьте в класс `Book` `@staticmethod` для проверки, является ли год выпуска 
допустимым (например, не отрицательным)
